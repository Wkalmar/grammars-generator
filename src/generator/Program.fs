open System

open Domain

[<EntryPoint>]
let main argv =
    // let items =
    //     generateProgression HarmonyItem.Tonic 5
    //     |> Array.map (createChordFromRootNote 60)
    Console.ReadLine() |> ignore
    0
